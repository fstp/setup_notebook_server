[Unit]
Description=Jupyter Lab Service
After=network.target

[Service]
Type=simple
User={{ jupyter_user }}
WorkingDirectory={{ jupyter_notebook_dir }}
ExecStart=/usr/local/bin/jupyter lab --config={{ jupyter_config_path }}
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
